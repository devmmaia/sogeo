build:
    docker:
        web: Dockerfile

release:
    image: web
    command:
        - python manage.py makemigrations
        - python manage.py migrate
run:
    web: gunicorn sogeo.wsgi:application --bind 0.0.0.0:$PORT

